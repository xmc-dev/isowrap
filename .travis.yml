language: go

sudo: required
services:
    - docker

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y asciidoc
    - git clone https://github.com/ioi/isolate
    - pushd isolate && make && sudo make install && popd
